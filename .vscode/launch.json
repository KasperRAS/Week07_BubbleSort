{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/bubble_sort.exe",
            "args": [],
            "preLaunchTask": "${defaultBuildTask}",
        }
    ],
    "version": "0.2.0",
}
